const SPRING_CONFIG = {
	damping: 24,
	stiffness: 280,
};

const DEFAULT_MIN_HEIGHT = 400;

const DRAG_CONSTRAINTS = {
	top: 0,
	left: 0,
	right: 0,
	bottom: 0,
};

export { SPRING_CONFIG, DEFAULT_MIN_HEIGHT, DRAG_CONSTRAINTS };
